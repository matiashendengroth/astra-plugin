---
description: Show CLAUDEX budget usage and limits for this project — /claudex-budget
---
Run exactly: `"${CLAUDE_PLUGIN_ROOT}/scripts/claudex" budget -C "$PWD"` and show its output verbatim in a code block. Add nothing.
