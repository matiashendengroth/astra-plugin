---
description: Show running CLAUDEX builders/reviews, recent runs with duration and tokens, and build worktrees — /claudex-status
---
Run exactly: `"${CLAUDE_PLUGIN_ROOT}/scripts/claudex" status -C "$PWD"` and show its output verbatim in a code block. Add nothing.
